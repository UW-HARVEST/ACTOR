//! A `Tree` names bytes that some step actually produced. If one could be built from a path, a
//! caller could feed a step a tree nothing produced -- which is the guarantee `SeededBy` used to
//! give and this replaces.
fn main() {
    let _forged = harvest_tools::tree::Tree {
        digest: unimplemented!(),
        at: unimplemented!(),
        _scratch: None,
    };
}
